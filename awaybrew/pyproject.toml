[project]
name = "awaybrew"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "msgpack>=1.1.2",
    "uvicorn[standard]>=0.40.0",
    "httpx >= 0.28.1",
    "python-dotenv >= 1.2.1",
    "firebase-admin >= 7.1.0",
    "project-cafe-utils",
]

[tool.uv.sources]
project-cafe-utils = { path = "../utils", editable = true }

[dependency-groups]
dev = [
    "ruff>=0.15.0",
]
