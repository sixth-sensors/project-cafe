[project]
name = "project-cafe-utils"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["fastapi>=0.128.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
